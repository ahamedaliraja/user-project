---
- name: User Creation
  hosts: dev
  vars_files:
    user_list.yml
  tasks:
    - name: User Creation Task
      ansible.builtin.user:
        name: "{{ item }}"
        group: devops
        state: present
      loop: "{{ users }}"
      when: "{{ item=='greg' }}"
