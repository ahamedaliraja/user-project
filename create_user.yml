---
- name: User creation
  hosts: prod
  vars_files:
    user_list.yml
  tasks:
    - name: User creation task
      ansible.builtin.user:
        name: "{{ item }}"
        group: devops
        state: present
      loop: "{{ users }}"
      when: "{{ item=='vision' }}" 
